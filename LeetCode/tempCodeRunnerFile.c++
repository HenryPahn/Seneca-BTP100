for(int i = 2; i < strs.size(); i++) {
        //     if(strs[i].substr(0, prefix.length()) != prefix) {
        //         int j = prefix.length();
        //         while(j > 0) {
        //             if(strs[i].substr(0, j) == prefix.substr(0, j)) {
        //                 prefix = prefix.substr(0, j);
        //                 break;
        //             }
        //             j--;
        //         }
        //         return "";
        //     }
        // }